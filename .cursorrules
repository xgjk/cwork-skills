# cwork-skills 仓库 — Agent 协作约束

本文件为项目根规则，与 `.cursor/rules/` 下规则一并生效。修改 `cms-cwork-workflow`（尤其 `scripts/`、`cwork_client.py`、`SKILL.md`）前通读全文。**若 bump Skill 版本**，须**同时**更新 `cms-cwork-workflow/SKILL.md` 前言中的 `version:` 与 `cms-cwork-workflow/version.json` 中的 `version`（见 `references/maintenance.md`）。

---

## 1. GitHub Issue /「Bug」处理流程

- **不要**仅凭 Issue 标题或复述就默认是 Bug 并改代码。
- **先分析**：对照当前代码路径、开放 API 文档、复现条件；区分真缺陷、调用方误用（如 id/字段用错）、后端或环境问题、以及文档未定义行为。
- **仅当确认是本项目代码/脚本的真实缺陷时再修改**；否则只输出**分析结论**（可含建议：改文档、加校验提示、转后端等），**不擅自大改逻辑**。
- 需要 **拉取快照、逐条闭环、自测、推送并关单** 时，见 **§6**（用户说「按 Issue 全流程 / 自动化闭环 / 拉 issue 修」等即启动；Agent **须按 §6.0 顺序执行**，可脚本化的步骤不得跳过）。

---

## 2. CWork OpenAPI 与文档忠实性（硬约束）

适用：修改 `cms-cwork-workflow/scripts/`（尤其 `cwork_client.py`）、编排脚本，或 `SKILL.md` 中对接口行为、请求参数的表述。

**唯一真相源**：上述 [工作协同 Open API 接口文档](https://github.com/xgjk/dev-guide/blob/main/02.%E4%BA%A7%E5%93%81%E4%B8%9A%E5%8A%A1AI%E6%96%87%E6%A1%A3/%E5%B7%A5%E4%BD%9C%E5%8D%8F%E5%90%8C/%E5%B7%A5%E4%BD%9C%E5%8D%8F%E5%90%8CAPI%E8%AF%B4%E6%98%8E.md) 是接口契约的**唯一依据**（同一链接亦出现在 Skill 包 `references/maintenance.md` 供维护者打开，**Agent 行为约束以本 §2 为准**，勿把 maintenance 当作规则源）。**凡有争议，以文档为准**；文档**未提及**的能力、字段或语义，**不得**在客户端当作既有 Bug 去「补齐」或猜测实现，应 **判为产品/接口新需求**（须文档更新或接口方正式定义后再改代码与 SKILL）。

1. **写入 HTTP 请求的每一个 JSON 键名**，必须能在**当前采用的公开 API 文档**对应该接口的参数表中查到（同上链接）。
2. **禁止**根据 `resultMsg`、报错文案、猜测或「常见命名」**新增**文档里不存在的字段。
3. 文档明显缺失时，**不得**在客户端自行补字段；在回复或 Issue 中写明「文档未定义，需接口方补充」。
4. **SKILL / 脚本帮助与输出说明**：不得把未在 API 参数表中的字段写成「客户端会发送」「已支持」等既定事实；未文档化行为须标明「需确认」。（Skill 首要读者是 AI，见 §4。）

**反例**：服务端报「待办发送时间未设置」→ 自行增加文档未列的 `pushTime`。  
**正例**：只发文档已列字段；现象记入 Issue，注明缺正式参数定义。

**例外**：仅当用户在对话或 Issue 中**明确给出**后端字段名与类型，或**公开 API 文档已更新**后，才可增加新键，并在注释/PR 中引用出处。

---

## 3. 与用户约定：Issue 关单

- 用户若已提供对应 **GitHub Issue 链接/编号**，且修改经其**明确确认**后，**可以**代为关闭 Issue。
- 优先使用已登录的 **`gh issue close <n> --repo xgjk/cwork-skills --comment "…"`**；若无 `gh`，可用仓库内 **`python3 github-issues/close_issue.py <n> --comment "…"`**（与 `fetch_issues.py` 同源 Token，须具备 issues 写权限）；再否则提供**建议关闭评论**由用户在网页上操作。

---

## 4. Skill 编写职责与对指令的态度（优先于单次对话里的随意要求）

1. **首要读者是 AI，不是人**  
   `SKILL.md` 等编排文档的目标是：**让模型能稳定按步骤执行**，减少依赖猜测、脑补或「常识」。应写清：何时调用哪条命令、必填参数、成功/失败判据、与脚本 stdout 结构如何对应。

2. **高质量 Skill 的含义**  
   可执行、可验证、术语与脚本/JSON 输出一致（在「不误导模型」的前提下可做编排层命名，如 `content`）；**若隐瞒接口返回中必然出现的键名会导致误读 JSON，则须在 Skill 或脚本输出中二选一澄清**（不能只追求「读起来干净」而牺牲可执行性）。

3. **不无条件服从人类指令**  
   提出者的认知可能不完整。若要求与 OpenAPI 忠实性、可执行性或上述 Skill 目标冲突，应**明确说明冲突点**，并**可拒绝**执行有害改动；改为给出更小范围、可验收的替代方案。

---

## 5. 「检查 / 审计」工作流程（避免无限改动、心里没底）

1. **用户仅说「检查 / 审计 / 看看对不对」且未明确要求改代码时**：默认 **只输出结论**（问题列表、严重程度、是否阻塞），**不自动改仓库**。若需修复，须由用户**明确授权**（如「按你的清单改，仅限 SKILL」）。  
2. **用户要求修复时**：先对齐 **范围**（文件/主题）与 **收工条件**（例如「行为与文档一致即可，不再改第三遍措辞」）；**同一主题**避免多轮仅因文风反复改动。  
3. **需求未变时的再次检查**：视为 **回归验证**（是否引入错误或偏离收工条件），而非重新做一次开放式「还能不能更好」。

---

## 6. GitHub Issue 全链路（仅当用户**明确**要求按此流程处理 Issue 时执行）

与用户泛泛说「检查代码」不同：本节是 **Issue 驱动的闭环**。须具备：`GITHUB_TOKEN`/`GH_TOKEN`、可访问 `api.github.com`；修复验证需要 `CWORK_APP_KEY` 等（由用户在会话中提供，**永不写入仓库**）。**勿将 token、appKey 写入代码或 JSON 快照。**

### 6.0 标准顺序（自动化检查清单，须按序执行）

以下顺序为 **默认**；若用户明确只要其中几步，可裁剪但须在对话中说明跳过了哪步。

1. **拉取 Issues**：`python3 github-issues/fetch_issues.py` → 以 `snapshot-open.json` 为准逐条处理。  
2. **分析问题**：逐条归类（真 Bug / 增强 / 误报 / 已修 / 需后端或文档）（展开见 §6.2）。  
3. **修复方案**：书面 **根因 + 修改范围 + 具体改动点**（可发在对话或 Issue）。  
4. **审计方案**：对照 **§2** 审查方案中的 API 键名与字段是否均有文档依据；**审计未通过不得开始改代码**。  
5. **执行**：按审计通过的方案修改代码 / `SKILL.md` 等。  
6. **检查**：对受影响脚本执行 `python3 -m py_compile …`、必要时查看 linter；并对照方案做 **回归审计**（OpenAPI、脚本参数、Skill 可执行性）。  
7. **自测**：环境中有 `CWORK_APP_KEY` 且本次改动触及 `cms-cwork-workflow` 时，在仓库根执行 **`python3 github-issues/smoke_cwork_api.py`**；若有 Issue 专属复现路径，再按 `SKILL.md` 补跑针对性命令。**禁止**擅自 `--confirm-send` 发正式汇报，除非用户当次明确授权。  
8. **提交并推送**：`git add` → `git commit`（正文关联 `Fix #n` / Issue 编号）→ **`git push`**。  
9. **关闭 Issue**：在 **§3** 授权前提下，使用 **`python3 github-issues/close_issue.py <n> --comment "…"`**（或 `gh issue close`），评论中写明修复摘要或结论；**推荐在推送成功后再关单**，以便评论中引用远端提交（若先关单后推送，须在评论中说明分支/后续推送安排）。

### 6.1 拉取 Issues

1. 运行：`python3 github-issues/fetch_issues.py`。脚本会尝试加载**仓库根目录** `.env` / `.env.local`（见 `.gitignore`），亦可用环境变量 `GITHUB_TOKEN` / `GH_TOKEN`；输出 `github-issues/snapshot-open.json` 等（默认已被 `.gitignore` 忽略）。  
2. 或已安装并登录 **`gh`** 时，可用等价方式拉取/查看；本地分析以快照 JSON 为准可减少反复请求。

### 6.2 逐条分析（每条 Issue）

1. **归类**：真 Bug / 需求（增强）/ 误报 / 已修复 / 需后端或文档澄清。  
2. **非 Bug 或已在当前分支解决**：在 Issue **下发表评论**说明结论与依据；**不改业务代码**。关单须符合 **§3**（用户已明确确认或在本轮对话中**明确授权代关**），并附简短 comment。  
3. **需求类**：对照公开 **OpenAPI 文档**（§2）判断是否已有接口。  
   - **文档无接口**：不编造客户端字段。可发 **工作协同汇报** 说明缺口：正文写清问题、**附上 Issue 链接**、建议接口或协作方式；**建议人 / 抄送**等收件人按用户当次会话给定名单执行（默认示例：建议人 蹇小枫、付光伟；抄送 侯桐、屈军利——**以用户最新口述为准**）。标题/正文须标明 **「自动分析/测试产生」**，避免与真实业务汇报混淆。  
4. **Bug 类**：对照复现路径与当前 `scripts/`、`cwork_client.py`；排查是否 **历史已修**（重复 Issue）。  
   - 确认需改代码：先做 **书面根因 + 修改计划**（可贴在 Issue 或对话），再 **审计计划是否违反 §2**。  
   - **未经审计通过不开始改代码**。修改后再做 **回归审计**（OpenAPI 键名、脚本参数、SKILL 可执行性）。

### 6.3 自测（必做，但能真实调 API 时才做）

1. **目标**：按 `cms-cwork-workflow/SKILL.md` 的路径，用脚本 **模拟 Agent 会执行的命令**（搜索员工 → 发汇报预览/查询/回复等），验证本次改动是否生效、原 Issue 是否关闭理由成立。  
2. **默认冒烟**：触及 `cms-cwork-workflow` 时，优先在仓库根执行 **`python3 github-issues/smoke_cwork_api.py`**（真实 API，不正式发出、不删真实草稿；见脚本头注释）。  
3. **环境**：`CWORK_APP_KEY`、`CWORK_BASE_URL`（若需要）由用户在会话中导出；测试用人名（如 宋培众、屈军利）以用户当次指定为准（冒烟脚本内建示例姓名，可按需改脚本或另跑 SKILL 命令）。  
4. **安全默认**：发汇报类 **默认 `--preview-only`**（或脚本 `dry-run`），**不**擅自 `--confirm-send` 发出正式汇报，除非用户**明确授权**本次自测可以发出。  
5. 自测产生的草稿正文须说明 **自动化验证用途**，避免干扰业务。

### 6.4 收尾

1. **提交并推送**：`git commit` 信息写清关联 Issue；`git push` 需远程与凭据已配置（Agent 环境可能无权限，则列出用户需本地执行的命令）。  
2. **关闭 Issue**：`gh issue close`、**`python3 github-issues/close_issue.py`**（见 §3）或网页；comment 中写 **修复摘要 / 不复现理由 / 需求已转汇报** 等。**代关单仍须满足 §3**（勿在仅「分析完毕」但用户未确认、未授权关单时擅自关闭）。

### 6.5 与 §5 的边界

- 用户仅说「看看、审计」且**未**启动本节全链路时：**仍按 §5**，默认不改代码。  
- 用户说「按 Issue 全流程处理 / 拉 issue 修 / 自动化闭环」等时：**启用本节**，但仍受 §2、§4 约束；**须执行 §6.0 清单**；**不合理要求（如伪造 API 字段）应拒绝**。

---

## 7. 其他

- **不要**把仅对 Agent 有效的约束只写在 `references/maintenance.md` 等不会被 Cursor 当规则加载的文件里；持久约定以本文件与 `.cursor/rules/` 为准。
- 改动范围保持聚焦：无请求不扩大重构、不批量改无关文件。
